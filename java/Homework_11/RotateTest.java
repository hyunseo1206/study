import javax.swing.*;

public class RotateTest extends JFrame{
	public RotateTest() {
		setSize(300, 330);
		setTitle("RotateTest : 2115411 CHS");
		JPanel panel = new RotatePanel();
		add(panel);
		setVisible(true);
	}
}
